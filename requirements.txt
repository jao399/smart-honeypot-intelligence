# Core requirements for the Smart Honeypot Intelligence app
# Tested with Python 3.8+ (Python 3.11 recommended)

streamlit>=1.20.0
pandas>=1.5.0
numpy>=1.24.0
plotly>=5.10.0
matplotlib>=3.6.0
seaborn>=0.12.0

# Notes:
# - These are conservative minimum versions; you can pin exact versions if you
#   need reproducible environments (e.g., streamlit==1.22.0).
# - Additional dependencies (dateutil, pytz, etc.) are pulled in automatically
#   by pandas and other packages when installed.
